 
 
// IDL file for one channel 

table MultichannelData {
    samples: [byte];
    nbr_channels: int;
    nbr_samples: int;
    timestamps: int64; 
    sample_rate: int;
    packetid: int;
    streamid: int;
}

root_type MultichannelData;
